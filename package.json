{
  "name": "knowbase",
  "type": "module",
  "version": "0.1.0",
  "description": "// knowbase — engineering notebook (Astro + TinaCMS + GitHub Pages)",
  "scripts": {
    "dev": "tinacms dev -c \"astro dev\"",
    "dev:astro": "astro dev",
    "build": "astro build && pagefind --site dist",
    "build:cloud": "tinacms build && astro build && pagefind --site dist",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.18",
    "astro": "^5.6.1"
  },
  "devDependencies": {
    "@astrojs/sitemap": "^3.3.0",
    "@tinacms/cli": "^1.9.5",
    "pagefind": "^1.5.2",
    "tinacms": "^2.7.5"
  }
}
